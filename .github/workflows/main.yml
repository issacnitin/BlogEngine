name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    
    - name: Run a multi-line script
      run: |
        echo "${{ secrets.SSH_PRIVATE_KEY }}" > beerwithai.key
        ls
        cat beerwithai.key
        scp -i beerwithai.key ./src/beerwithai.com.conf issacnitin@70.37.72.51:/etc/nginx/sites-available/
